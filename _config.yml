# Site settings
title: "My Portfolio"
email: ""
description: "Assignments and projects organized by grade and subject"
baseurl: "" # keep empty if hosted at https://jas0nmc.github.io
url: "https://jas0nmc.github.io"

# Theme
theme: minimal-mistakes-jekyll

# Build settings
sass:
  style: compressed

plugins:
  - jekyll-feed

# Defaults for posts
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: single

# Optional: Navigation (can also be in _data/navigation.yml)
# We are keeping navigation in _data/navigation.yml for clarity
